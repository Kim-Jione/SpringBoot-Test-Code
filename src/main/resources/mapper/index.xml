<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.test.domain.index.IndexDao">

	<insert id="insert">
		INSERT INTO users(username, password, email,
		createdAt)
		VALUES(#{username}, #{password}, #{email}, now())
	</insert>

	<select id="findAll"
		resultType="com.example.test.domain.index.Index">
	</select>

	<select id="findById"
		resultType="com.example.test.domain.index.Index">
	</select>

	<update id="update">
	</update>

	<delete id="deleteById">
	</delete>



</mapper>